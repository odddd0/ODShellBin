#!/bin/bash

# $1: file name
# $2: key

if [ "$1" == "" -o "$2" == "" ] ; then
    echo 'put on'
    echo '$1: key'
    echo '$2: file name'
else
    tar -cf - "$2" | openssl des3 -salt -k $1 | dd of="$2.out"
fi
