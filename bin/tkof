#!/bin/bash

# $1: file name
# $2: key

if [ "$1" == "" -o "$2" == "" ] ; then
    echo 'take off'
    echo '$1: key'
    echo '$2: file name'
else
    dd if="$2" | openssl des3 -d -k $1 | tar -xf -
fi
